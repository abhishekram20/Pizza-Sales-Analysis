# Cleaned Data

For this project, the original CSV dataset did not require data cleaning or preprocessing.

The dataset was opened in Microsoft Excel and saved as an Excel (.xlsx) file before being imported into SQL Server Management Studio (SSMS) for analysis.

No missing values, duplicate records, or formatting issues were identified that required modification.

This folder contains the Excel version of the dataset used throughout the project.